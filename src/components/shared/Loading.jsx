import loader from "../assets/loading.gif"

const Loading = () => {
  return <img src={loader} alt="Loading..." style={{ width: "50px", margin: "auto", display: "block" }} />
}

export default Loading
